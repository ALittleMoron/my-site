name: "Deploy to production"

on:
  workflow_dispatch:

jobs:
  deploy:
    name: "Deploy to production"
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/_deploy.yaml
    secrets: inherit
