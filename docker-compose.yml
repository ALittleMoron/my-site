services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: ${DOCKER_REGISTRY}/${DOCKER_USERNAME}/my_site_application:${IMAGE_TAG:-latest}
    container_name: my_site_backend
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: my_site_frontend:latest
    container_name: my_site_frontend
    environment:
      - PORT=4000
      - APP_URL_SCHEMA=${APP_URL_SCHEMA}
      - APP_DOMAIN=${APP_DOMAIN}
      - SSR_API_ORIGIN=http://backend:8080
      - SSR_PUBLIC_ORIGIN=${APP_URL_SCHEMA}://${APP_DOMAIN}
      - NG_ALLOWED_HOSTS=${APP_DOMAIN}
    restart: unless-stopped
    networks:
      - app-network

  valkey:
    image: valkey/valkey:9.0.1
    container_name: my_site_valkey
    volumes:
      - valkey_data:/data
    networks:
      - app-network

  postgres:
    image: postgres:18.4-alpine
    container_name: my_site_postgres
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 5s
      timeout: 60s
      retries: 12

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: my_site_minio
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 60s
      retries: 12

  nginx:
    build:
      context: .
      dockerfile: infra/nginx/Dockerfile
    image: my_site_nginx:latest
    container_name: my_site_nginx
    volumes:
      - ./infra/nginx/certs:/certs
    environment:
      - APP_DOMAIN=${APP_DOMAIN}
      - SSL_CERT=${SSL_CERT}
      - SSL_KEY=${SSL_KEY}
    ports:
      - "80:8080"
      - "443:8443"
      - "${VPN_BIND_ADDRESS}:18081:18081"
      - "${VPN_BIND_ADDRESS}:18082:18082"
    depends_on:
      - backend
      - frontend
    networks:
      - app-network

  certbot:
    image: certbot/certbot:v5.2.2
    container_name: my_site_certbot
    profiles:
      - letsencrypt
    volumes:
      - letsencrypt:/etc/letsencrypt
      - certbot-www:/var/www/certbot
    command: >
      certonly
      --webroot
      --webroot-path=/var/www/certbot
      --email ${LE_EMAIL}
      --agree-tos
      --no-eff-email
      -d ${APP_DOMAIN}
      -d s3.${APP_DOMAIN}
    networks:
      - app-network

  databasus:
    container_name: my_site_databasus
    image: databasus/databasus:latest
    volumes:
      - databasus_data:/databasus-data
    restart: unless-stopped
    networks:
      - app-network
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
  minio_data:
  databasus_data:
  valkey_data:
  letsencrypt:
  certbot-www:
